import datetime

time_log = True 
default_get_timerange = datetime.timedelta(minutes=60)
host_timeout = datetime.timedelta(minutes=5)
default_get_sampling = 60
query_rows_limit = 100
session_key_length = 8
aggregate_l1 = datetime.timedelta(minutes=20)
aggregate_l2 = datetime.timedelta(hours=2)
aggregate_cleanup = datetime.timedelta(days=7)
